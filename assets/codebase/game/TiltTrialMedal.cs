namespace city.game {
    /// <summary>
    /// Stores the awarded Tilt Trial result tier for one completed run.
    /// </summary>
    public enum TiltTrialMedal {
        None = 0,
        Bronze = 1,
        Silver = 2,
        Gold = 3
    }
}
