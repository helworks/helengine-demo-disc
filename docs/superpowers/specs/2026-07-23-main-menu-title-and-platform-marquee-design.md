# Main Menu Title and Platform Marquee Design

The standard menu top-right overlay displays `HELENGINE` over `DEMO DISC` instead of platform metadata. The footer marquee keeps `MADE BY HELENA / HELEN OF CODE` and appends the current runtime platform name and version, uppercased, after a slash. The handheld menu is unchanged.
